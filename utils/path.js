const root = require("./root.js")

module.exports = {
    root: require("./root.js"),
    assets: `${root}/assets`,
    commands: `${root}/commands`,
    data: `${root}/data`,
    events: `${root}/events`,
    utils: `${root}/utils`,
    temp: `${root}/temp`
}